#!/usr/bin/env bash
#MISE description="Render the Copier template against fixtures and validate the output"
set -euo pipefail
cd "$(dirname "$0")/.."
uv run --with-requirements test/requirements-render-check.txt pytest -n auto test/test_render_check.py "$@"
