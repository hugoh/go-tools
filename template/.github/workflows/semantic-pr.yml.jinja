# DO NOT EDIT — managed by hugoh/go-tools via copier.
---
name: semantic-pr

on: # yamllint disable-line rule:truthy
  pull_request:
    types: [opened, edited, reopened, synchronize]

permissions: {}

jobs:
  semantic-pr:
    uses: hugoh/go-tools/.github/workflows/semantic-pr.yml@[[ _copier_conf.vcs_ref_hash ]] # [[ _copier_answers['_commit'] ]]
    permissions:
      pull-requests: read
