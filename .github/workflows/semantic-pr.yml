---
name: semantic-pr

on: # yamllint disable-line rule:truthy
  pull_request:
    types: [opened, edited, reopened, synchronize]

permissions: {}

jobs:
  semantic-pr:
    uses: ./.github/workflows/go-semantic-pr.yml
    permissions:
      pull-requests: read
